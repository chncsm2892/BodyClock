# FastFlow

Simple fasting and weight tracker built with React + Vite.

This version saves active fasting state and weight logs in localStorage, so the timer continues correctly after closing and reopening the browser.

## Install

npm install

## Run

npm run dev

## Build

npm run build
